# V0.6.5 Runtime Configuration Plan

## Status

Implemented on 2026-05-19. Durable outcome and verification are recorded in `docs/changes.md`.

## Completed Steps

1. Add `config/app.yaml` with default local path settings, filename templates, and disabled Obsidian scaffolding.
2. Add `src.app.config` and `src.app.runtime` for `RuntimeConfig`, `AppPaths`, path resolution, environment overrides, and filename construction.
3. Refactor `src.main` so `run_pipeline(runtime)` owns orchestration and `main(argv)` only handles CLI config loading.
4. Refactor `src.report.weekly_candidates` so default export/report paths and report filename templates come from runtime config.
5. Refactor `src.diagnostics` so default database and export paths come from runtime config while preserving explicit test parameters.
6. Add temp-directory tests for default resolution, relative and absolute paths, environment overrides, pipeline outputs, report output, and diagnostics.
7. Update maintained docs to describe the new V0.6.5 runtime configuration boundary.

## If Interrupted

The implementation is complete. Resume by reading `docs/changes.md` for the V0.6.5 entry, then run the validation commands from `docs/testing.md`.
