# V0.7.1 Score Audit Calibration Plan

## Status

Implemented on 2026-05-19. Durable outcome and verification are recorded in `docs/changes.md`.

## Completed Steps

1. Add `score_audit_filename_template` to `config/app.yaml`.
2. Add `score_audit_path()` to `src.app.runtime`.
3. Add `src.report.score_audit` with `build_score_audit()`, `render_score_audit()`, `generate_score_audit_report()`, and CLI entrypoint.
4. Reuse existing JSONL loading, scoring, and lane balance behavior.
5. Add compact Markdown sections for summary, candidate levels, lane stats, source stats, component stats, penalties, near-threshold candidates, top/bottom selected candidates, rejected/downranked summary, optional no-scoring comparison, and calibration notes.
6. Add offline tests for report generation, stats sections, near-threshold candidates, calibration notes, and runtime reports directory behavior.
7. Archive the V0.7 active spec/plan and update maintained docs.

## If Interrupted

The implementation is complete. Resume by reading `docs/changes.md` for the V0.7.1 entry, then run the validation commands from `docs/testing.md`.
