# V0.7 Scoring, Balance, and Rejects Plan

## Status

Implemented on 2026-05-19. Durable outcome and verification are recorded in `docs/changes.md`.

## Completed Steps

1. Add `config/scoring.yaml` with default rule weights, thresholds, lane balance, reject behavior, report behavior, and configurable signal terms.
2. Extend runtime config with `scoring_config_path` and merged scoring config fallback behavior.
3. Add `src.ranking.scoring` for immutable score results and candidate scoring.
4. Add `src.ranking.balance` for duplicate handling, lane balance, selected candidates, rejected candidates, and downranked candidates.
5. Add `src.ranking.rejects` for reject/downrank Markdown rendering.
6. Update weekly report generation to score candidates by default, keep `--no-scoring`, and add `--write-reject-log`.
7. Add tests for scoring, lane relevance, review/survey signals, benchmark penalties, duplicate handling, lane balance, reject logs, report rendering, no-scoring compatibility, and runtime output paths.
8. Archive the V0.6.5 active spec/plan and update maintained docs.

## If Interrupted

The implementation is complete. Resume by reading `docs/changes.md` for the V0.7 entry, then run the validation commands from `docs/testing.md`.
