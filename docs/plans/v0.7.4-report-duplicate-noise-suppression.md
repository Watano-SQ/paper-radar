# V0.7.4 Report Duplicate and Repository Noise Suppression Plan

## Status

Implemented on 2026-05-23. Durable outcome and verification are recorded in `docs/changes.md`.

## Completed Steps

1. Add repository-like penalty configuration to `config/scoring.yaml`.
2. Add repository-like signal detection to report-stage scoring.
3. Add near-duplicate title suppression to balanced selection.
4. Downrank suppressed title duplicates with reason `Near-duplicate title`.
5. Keep source crawling, SQLite schema, JSONL export format, and source enablement unchanged.
6. Add focused tests for duplicate-title suppression, arXiv/OpenAlex mirror preference, repository-like penalties, and reject/downrank log visibility.
7. Archive the V0.7.3 active spec/plan and update maintained docs.

## If Interrupted

The implementation is complete. Resume by reading the V0.7.4 entry in `docs/changes.md`, then run the validation commands from `docs/testing.md`.
