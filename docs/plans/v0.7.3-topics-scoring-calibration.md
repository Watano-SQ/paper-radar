# V0.7.3 Topics and Scoring Calibration Plan

## Status

Implemented on 2026-05-23. Durable outcome and verification are recorded in `docs/changes.md`.

## Completed Steps

1. Review the 2026-W21 candidate export, weekly report, reject/downrank log, and score audit.
2. Replace broad first keywords in `config/topics.yaml` with narrower default crawl seeds.
3. Keep broad keywords lower in each lane when still useful for later explicit expansion.
4. Raise `s_candidate_min` in `config/scoring.yaml`.
5. Reduce `metadata.has_abstract` and strengthen `missing_abstract_penalty`.
6. Keep `config/sources.yaml` unchanged.
7. Run source preview and the pytest suite with a repository-external basetemp.
8. Archive the V0.7.2 active spec/plan and update maintained docs.

## If Interrupted

The implementation is complete. Resume by reading the V0.7.3 entry in `docs/changes.md`, then run the validation commands from `docs/testing.md`.
